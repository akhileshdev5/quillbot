apiVersion: v1
kind: Service
metadata:
  name: quillbot-service
spec:
  type: NodePort
  selector:
    app: quillbot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30909
